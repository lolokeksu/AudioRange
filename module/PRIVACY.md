# Privacy

AudioRange contains no telemetry, analytics, advertising, remote code loader or background network client.

All compatibility records, history and reports remain on the device under `/data/adb/audiorange`. A generated diagnostic report may contain the device model, Android API level, root manager and exact firmware fingerprint. Review the report before posting it publicly.
